@model TicketsTrainDomain.Model.TrainAtRoute

@{
    ViewData["Title"] = "TrainAtRoute - Details";
}

<h1>Деталі</h1>

<dl class="row">
    <dt class="col-sm-2">Потяг</dt>
    <dd class="col-sm-10">@Model.Train?.TrainName</dd>

    <dt class="col-sm-2">Маршрут</dt>
    <dd class="col-sm-10">
        @($"{Model.Route?.StartStation} - {Model.Route?.EndStation}")
    </dd>
</dl>

<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Назад</a>
</div>
